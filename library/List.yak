# Needs list random access ASAP
List.instance.join = {
    [@self, [@s]]:
        result = ''
        i = 1
        {[]: i < @self.length}.while-true {
            []:
                result |= result + @s + @self[i].to-string
                i |= i + 1
        }
        @self[0] + result
}
