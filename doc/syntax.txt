<Number> = (Number <internal>)

<String> = (String <internal>)

<Boolean> = (Boolean <internal>)

<Nil> = (Nil)

<Dot> = (Dot)

<Unknown> = (Unknown)

<Identifier> = (Identifier <internal>)

<Matched-identifier> = (Matched-identifier <internal>)

<List-literal> = (List-literal (<Exp>...))

<Funject-literal> = (Funject-literal ((<Delayed-exp> <Delayed-exps>)...))

    <Delayed-exp> = (Delayed-exp <Exp>)
    
    <Delayed-exps> = (Delayed-exps (<Delayed-exp>...))

<Strict-assignment>  = (Strict-assignment>  <Exp> <Exp>)

<Delayed-assignment> = (Delayed-assignment> <Exp> <Delayed-exps>)

<Reset-strict-assignment> = (Reset-strict-assignment> <Exp> <Exp>)

<Reset-delayed-assignment> = (Reset-delayed-assignment> <Exp> <Delayed-exps>)

<Funject-strict-assignment> = (Funject-strict-assignment> <Exp> <Delayed-exps>)

<Funject-delayed-assignment> = (Funject-delayed-assignment> <Exp> <Delayed-exps>)

<Invocation> = (Invocation <internal>)

<Funject-inheritance> = (Funject-inheritance <Exp> <Exp>)

<Inverse-definition> = (Inverse-definition <Exp> <Exp>)


